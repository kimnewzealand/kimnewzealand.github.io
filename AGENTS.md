# Agent rules

- Never run git add, git commit, git push, git pull, git fetch, git merge, git rebase, git branch, git checkout, git switch, git reset, git revert, git stash, or any other git operation.
- If you need to make changes, write them to the filesystem. Do not stage, commit, or push them.
